[project]
name = "jetq"
version = "0.1.0"
description = "A Python implementation of .NET's Language Integrated Query (LINQ), providing a functional, fluent API for querying and manipulating collections."
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
